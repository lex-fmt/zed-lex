# Generated by release-sync from arthur-debert/release@48d4373789d4. Do not edit.
# Regenerate by running release-sync.

output:
  - meta
  - skips
  - summary
pre-commit:
  parallel: true
  commands:
    markdownlint:
      glob: "**/*.md"
      run: >-
        if command -v markdownlint >/dev/null 2>&1; then

          markdownlint --fix -- {staged_files};
        else

          echo "markdownlint not installed (skipping; install via npm i -g markdownlint-cli or brew install markdownlint-cli)" >&2;
        fi
      stage_fixed: true
      priority: 2
    yamllint:
      glob: "**/*.{yml,yaml}"
      run: >-
        if command -v yamllint >/dev/null 2>&1; then

          yamllint {staged_files};
        else

          echo "yamllint not installed (skipping; install via pip install yamllint or brew install yamllint)" >&2;
        fi
      priority: 2
    shellcheck:
      glob:
        - "**/*.{sh,bash}"
        - "scripts/**"
        - "bin/**"
        - "app-bin/**"
      run: bin/check-shell {staged_files}
      priority: 2
    cargo-fmt:
      glob:
        - "**/*.rs"
        - "**/rustfmt.toml"
      run: cargo fmt --all -- --check
      priority: 2
    cargo-clippy-wasm:
      glob:
        - "**/*.rs"
        - "**/Cargo.toml"
        - "**/Cargo.lock"
      run: cargo clippy --release --all-features --target wasm32-wasip2 -- -D warnings
      priority: 2
