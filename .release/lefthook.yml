# Generated by release-sync from arthur-debert/release@264975ee4f77. Do not edit.
# Regenerate by running release-sync.

output:
  - meta
  - skips
  - summary
pre-commit:
  parallel: true
  commands:
    markdownlint:
      glob: "**/*.md"
      run: >-
        if command -v markdownlint >/dev/null 2>&1; then

          markdownlint --fix -- {staged_files};
        else

          echo "ERROR: markdownlint not installed — the gate does not skip. Re-run the bootstrap (setup-dev-env.sh)." >&2;
          exit 1;
        fi
      stage_fixed: true
      priority: 2
    yamllint:
      glob: "**/*.{yml,yaml}"
      run: >-
        if command -v yamllint >/dev/null 2>&1; then

          yamllint -- {staged_files};
        else

          echo "ERROR: yamllint not installed — the gate does not skip. Re-run the bootstrap (setup-dev-env.sh)." >&2;
          exit 1;
        fi
      priority: 2
    shellcheck:
      glob:
        - "**/*.{sh,bash}"
        - "bin/**"
        - "app-bin/**"
      run: bin/check-shell {staged_files}
      priority: 2
    cargo-fmt:
      glob:
        - "**/*.rs"
        - "**/rustfmt.toml"
      run: cargo fmt --all -- --check
      priority: 2
    cargo-clippy-wasm:
      glob:
        - "**/*.rs"
        - "**/Cargo.toml"
        - "**/Cargo.lock"
      run: cargo clippy --release --all-features --target wasm32-wasip2 -- -D warnings
      priority: 2
