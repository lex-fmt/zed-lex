- Delete the dead `tree-sitter` / `tree-sitter-repo` pin from `shared/lex-deps.json`. It was never read (the deserializing struct in `src/lex.rs` declares only the `lexd-lsp` keys) and had gone two minor versions stale, making it a trap for anyone auditing tree-sitter-lex consumers by grepping for pins. The grammar's only pin is `[grammars.lex] commit` in `extension.toml`, which Zed clones and builds itself.
- The bats grammar-download fallback (`test/helpers.bash`) now resolves tree-sitter-lex from that `[grammars.lex]` repository + commit instead of the deleted pin, so the query tests run against exactly the grammar Zed builds.
