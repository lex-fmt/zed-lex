- Remove stale `app-bin/` scripts (`build`, `pre-commit`, `test-all`); canonical entry points are now `bin/build` and `bin/check`
- Pre-commit is handled by `lefthook.yml` (generated by release-sync) instead of a hand-rolled hook
